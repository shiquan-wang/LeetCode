#include<iostream>
#include<vector>
#include<string>
#include<unordered_set>

using namespace std;

class  Solution{
public:
    int get_max_length(string& str){
        int n = str.size();
        unordered_set<char> st;
        int left = 0, right = 0;
        for(; left < n; ++left){
            st.insert(str[left]);
            for()

        }
    }
};